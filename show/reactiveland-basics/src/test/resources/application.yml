spring:
  application:
    name: realtiveland
#
  r2dbc:
      url: r2dbc:postgres://${postgres.username}:${postgres.password}@localhost:${postgres.port}/${postgres.db}
      username: ${postgres.username}
      password: ${postgres.password}

#  flyway:
#    url: ${postgres.url}
#    user: ${postgres.username}
#    password: ${postgres.password}
#    baseline-on-migrate: true
#    enabled: true
#
#  datasource:
#    url: ${postgres.url}
#    user: ${postgres.username}
#    password: ${postgres.password}
